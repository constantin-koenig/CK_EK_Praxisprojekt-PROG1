<Window x:Class="ArchivSoftware.Ui.Views.ImportLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Auto-Import Log" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap">
            <Run Text="Auto-Import Verzeichnis: "/>
            <Run Text="{Binding ImportPath}" FontWeight="Bold"/>
        </TextBlock>
        
        <ListView x:Name="LogListView" 
                  Grid.Row="1" 
                  ItemsSource="{Binding ImportLog}"
                  BorderThickness="1"
                  BorderBrush="LightGray">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Zeit" Width="80" DisplayMemberBinding="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"/>
                    <GridViewColumn Header="Status" Width="120" DisplayMemberBinding="{Binding StatusText}"/>
                    <GridViewColumn Header="Datei" Width="150" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="Nachricht" Width="200" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Schließen" Padding="20,5" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
